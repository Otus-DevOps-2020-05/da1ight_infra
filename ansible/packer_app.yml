---
- name: Install Ruby and Bundler
  hosts: all
  become: true
  tasks:
    - name: Apt-get update
      apt:
        update_cache: yes

    - name: Install packages
      apt: "name={{ item }} state=present"
      with_items:
        - git
        - ruby-full
        - ruby-bundler
        - build-essential
